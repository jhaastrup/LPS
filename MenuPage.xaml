<Page
    x:Class="LPS.MenuPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LPS"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="SkyBlue">

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="350" />
                <ColumnDefinition Width="auto"/>

            </Grid.ColumnDefinitions>



            <StackPanel Grid.Column="0" Orientation="Vertical" Width="250"
                        VerticalAlignment="Center" Background="Snow" Height="850" Margin="-100,0,0,0">


                <HyperlinkButton Content="Profile" Height="220" Width="257" FontSize="30" Foreground="Black" Click="profileBtn" Margin="0,0,-7,0"/>


                <HyperlinkButton Content="Loan" Height="208" Width="257" FontSize="30" Foreground="Black" Click="loanBtn" Margin="0,0,-7,0" />

                <!--   <Frame x:Name="frmlogout"> -->
                <HyperlinkButton Content="Logout" Height="224" Width="257" FontSize="30" Click="logoutBtn" Foreground="Black" Margin="0,0,-7,0"/>
            </StackPanel>

            <Grid Grid.Column="1">
                <Frame x:Name="frmProfile" Grid.Column="1">
                </Frame>
            </Grid>


        </Grid>
    </Grid>
</Page>
